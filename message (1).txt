<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Top Cheats</title>
<link href="https://fonts.googleapis.com/css2?family=Rajdhani:wght@500;600;700&family=Inter:wght@300;400;500;600&display=swap" rel="stylesheet">
<style>
  :root {
    --green: #3dff6e;
    --green-dim: #1a8f3a;
    --green-glow: rgba(61,255,110,0.12);
    --bg: #080808;
    --bg2: #0d0d0d;
    --bg3: #111111;
    --card: #0e0e0e;
    --border: #1a1a1a;
    --text: #e8e8e8;
    --muted: #444;
    --sub: #2a2a2a;
  }

  * { box-sizing: border-box; margin: 0; padding: 0; }

  body {
    background: var(--bg);
    color: var(--text);
    font-family: 'Inter', sans-serif;
    overflow-x: hidden;
  }

  /* grid bg */
  body::before {
    content: '';
    position: fixed;
    inset: 0;
    background-image:
      linear-gradient(rgba(61,255,110,0.02) 1px, transparent 1px),
      linear-gradient(90deg, rgba(61,255,110,0.02) 1px, transparent 1px);
    background-size: 60px 60px;
    pointer-events: none;
    z-index: 0;
  }

  /* radial glow behind hero */
  .hero-glow {
    position: absolute;
    top: -120px;
    left: 50%;
    transform: translateX(-50%);
    width: 700px;
    height: 500px;
    background: radial-gradient(ellipse at center, rgba(61,255,110,0.07) 0%, transparent 70%);
    pointer-events: none;
    z-index: 0;
  }

  /* ── NAV ── */
  nav {
    position: relative;
    z-index: 10;
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 0 48px;
    height: 58px;
    background: rgba(8,8,8,0.95);
    border-bottom: 1px solid var(--border);
    backdrop-filter: blur(8px);
  }

  .nav-logo {
    font-family: 'Rajdhani', sans-serif;
    font-size: 22px;
    font-weight: 700;
    color: var(--text);
    letter-spacing: 2px;
    text-transform: uppercase;
  }

  .nav-logo span { color: var(--green); }

  .nav-links {
    display: flex;
    align-items: center;
    gap: 36px;
  }

  .nav-links a {
    font-size: 13px;
    font-weight: 500;
    color: var(--muted);
    text-decoration: none;
    letter-spacing: 1px;
    text-transform: uppercase;
    transition: color .2s;
    display: flex;
    align-items: center;
    gap: 6px;
  }

  .nav-links a:hover { color: var(--text); }

  .nav-right {
    display: flex;
    align-items: center;
    gap: 10px;
  }

  .nav-icon {
    width: 36px;
    height: 36px;
    border: 1px solid var(--border);
    border-radius: 6px;
    background: var(--bg2);
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    transition: border-color .2s;
    color: var(--muted);
    font-size: 15px;
  }

  .nav-icon:hover { border-color: var(--green); color: var(--green); }

  /* ── HERO ── */
  .hero {
    position: relative;
    text-align: center;
    padding: 90px 20px 80px;
    overflow: hidden;
  }

  .hero h1 {
    font-family: 'Rajdhani', sans-serif;
    font-size: 72px;
    font-weight: 700;
    letter-spacing: 3px;
    line-height: 1;
    margin-bottom: 18px;
    position: relative;
    z-index: 1;
  }

  .hero h1 span { color: var(--green); }

  .hero p {
    font-size: 15px;
    color: var(--muted);
    margin-bottom: 36px;
    letter-spacing: 1px;
    position: relative;
    z-index: 1;
  }

  .search-bar {
    position: relative;
    z-index: 1;
    max-width: 480px;
    margin: 0 auto 32px;
  }

  .search-bar input {
    width: 100%;
    background: var(--bg2);
    border: 1px solid var(--border);
    border-radius: 8px;
    padding: 13px 18px 13px 46px;
    color: var(--text);
    font-size: 14px;
    font-family: 'Inter', sans-serif;
    outline: none;
    transition: border-color .2s;
  }

  .search-bar input::placeholder { color: var(--muted); }
  .search-bar input:focus { border-color: var(--green-dim); }

  .search-bar svg {
    position: absolute;
    left: 16px;
    top: 50%;
    transform: translateY(-50%);
    color: var(--muted);
  }

  .hero-btns {
    position: relative;
    z-index: 1;
    display: flex;
    justify-content: center;
    gap: 12px;
    margin-bottom: 64px;
  }

  .btn-primary {
    background: var(--bg2);
    border: 1px solid var(--border);
    color: var(--text);
    font-size: 13px;
    font-weight: 600;
    font-family: 'Inter', sans-serif;
    letter-spacing: 1px;
    padding: 11px 28px;
    border-radius: 8px;
    cursor: pointer;
    text-transform: uppercase;
    transition: border-color .2s, background .2s;
  }

  .btn-primary:hover { border-color: var(--green); background: #111; }

  .btn-green {
    background: var(--green);
    border: 1px solid var(--green);
    color: #000;
    font-size: 13px;
    font-weight: 700;
    font-family: 'Inter', sans-serif;
    letter-spacing: 1px;
    padding: 11px 28px;
    border-radius: 8px;
    cursor: pointer;
    text-transform: uppercase;
    transition: background .2s;
  }

  .btn-green:hover { background: #2acc58; }

  /* ── STATS ── */
  .stats {
    position: relative;
    z-index: 1;
    display: flex;
    justify-content: center;
    gap: 12px;
    flex-wrap: wrap;
  }

  .stat-card {
    background: var(--card);
    border: 1px solid var(--border);
    border-radius: 10px;
    padding: 20px 32px;
    text-align: center;
    min-width: 130px;
  }

  .stat-num {
    font-family: 'Rajdhani', sans-serif;
    font-size: 32px;
    font-weight: 700;
    color: var(--green);
    letter-spacing: 1px;
  }

  .stat-lbl {
    font-size: 11px;
    color: var(--muted);
    letter-spacing: 2px;
    text-transform: uppercase;
    margin-top: 2px;
  }

  /* ── FEATURED ── */
  .featured-section {
    max-width: 1100px;
    margin: 80px auto 0;
    padding: 0 24px;
  }

  .featured-card {
    background: var(--card);
    border: 1px solid var(--border);
    border-radius: 14px;
    padding: 48px;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 48px;
    align-items: center;
    position: relative;
    overflow: hidden;
  }

  .featured-card::before {
    content: '';
    position: absolute;
    top: 0; left: 0; right: 0;
    height: 1px;
    background: linear-gradient(90deg, transparent, var(--green), transparent);
  }

  .feat-badge {
    display: inline-block;
    background: rgba(61,255,110,0.1);
    border: 1px solid var(--green-dim);
    color: var(--green);
    font-size: 11px;
    font-weight: 600;
    letter-spacing: 2px;
    padding: 4px 14px;
    border-radius: 20px;
    text-transform: uppercase;
    margin-bottom: 18px;
  }

  .feat-title {
    font-family: 'Rajdhani', sans-serif;
    font-size: 42px;
    font-weight: 700;
    color: var(--text);
    letter-spacing: 2px;
    text-transform: uppercase;
    margin-bottom: 8px;
  }

  .feat-sub {
    font-size: 13px;
    color: var(--muted);
    margin-bottom: 28px;
    letter-spacing: 1px;
  }

  .feat-tags {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 10px;
    margin-bottom: 32px;
  }

  .feat-tag {
    background: var(--bg2);
    border: 1px solid var(--border);
    border-radius: 8px;
    padding: 11px 16px;
    font-size: 13px;
    color: var(--text);
    display: flex;
    align-items: center;
    gap: 10px;
  }

  .feat-tag svg { color: var(--green); flex-shrink: 0; }

  .feat-price-row {
    display: flex;
    align-items: center;
    gap: 20px;
  }

  .feat-price {
    font-family: 'Rajdhani', sans-serif;
    font-size: 38px;
    font-weight: 700;
    color: var(--green);
  }

  .feat-img {
    border-radius: 10px;
    border: 1px solid var(--border);
    background: var(--bg2);
    aspect-ratio: 16/9;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
  }

  .feat-img-placeholder {
    width: 100%;
    height: 100%;
    background: var(--bg3);
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 10px;
    color: var(--sub);
    font-size: 12px;
    letter-spacing: 2px;
    text-transform: uppercase;
  }

  /* ── FEATURES STRIP ── */
  .features-strip {
    max-width: 1100px;
    margin: 20px auto 0;
    padding: 0 24px;
  }

  .features-box {
    background: var(--card);
    border: 1px solid var(--border);
    border-radius: 14px;
    padding: 36px 48px;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 32px;
    align-items: center;
  }

  .feat-left h3 {
    font-family: 'Rajdhani', sans-serif;
    font-size: 26px;
    font-weight: 700;
    color: var(--text);
    text-transform: uppercase;
    letter-spacing: 2px;
    margin-bottom: 10px;
  }

  .feat-left p {
    font-size: 13px;
    color: var(--muted);
    line-height: 1.7;
  }

  .feat-right {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 12px;
  }

  .feat-item {
    display: flex;
    align-items: center;
    gap: 10px;
    font-size: 13px;
    color: var(--text);
    font-weight: 500;
  }

  .feat-dot {
    width: 28px;
    height: 28px;
    border-radius: 50%;
    background: rgba(61,255,110,0.1);
    border: 1px solid var(--green-dim);
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
  }

  .feat-dot svg { color: var(--green); }

  /* ── PRODUCTS ── */
  .products-section {
    max-width: 1100px;
    margin: 60px auto 0;
    padding: 0 24px;
  }

  .section-header {
    margin-bottom: 28px;
  }

  .section-header h2 {
    font-family: 'Rajdhani', sans-serif;
    font-size: 28px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 3px;
    color: var(--text);
  }

  .section-header p {
    font-size: 13px;
    color: var(--muted);
    margin-top: 4px;
  }

  .products-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
    gap: 14px;
  }

  .product-card {
    background: var(--card);
    border: 1px solid var(--border);
    border-radius: 12px;
    padding: 24px 20px;
    display: flex;
    flex-direction: column;
    gap: 0;
    transition: border-color .2s, transform .2s;
    cursor: pointer;
  }

  .product-card:hover {
    border-color: #2a2a2a;
    transform: translateY(-2px);
  }

  .prod-tag {
    font-size: 10px;
    color: var(--green-dim);
    letter-spacing: 2px;
    text-transform: uppercase;
    font-weight: 600;
    margin-bottom: 12px;
  }

  .prod-name {
    font-family: 'Rajdhani', sans-serif;
    font-size: 24px;
    font-weight: 700;
    color: var(--text);
    text-transform: uppercase;
    letter-spacing: 1px;
    margin-bottom: 6px;
  }

  .prod-desc {
    font-size: 12px;
    color: var(--muted);
    line-height: 1.5;
    margin-bottom: 20px;
    flex: 1;
  }

  .prod-price {
    font-family: 'Rajdhani', sans-serif;
    font-size: 26px;
    font-weight: 700;
    color: var(--green);
    margin-bottom: 2px;
  }

  .prod-price span {
    font-size: 12px;
    color: var(--sub);
    font-weight: 400;
    font-family: 'Inter', sans-serif;
  }

  .prod-per {
    font-size: 11px;
    color: var(--sub);
    margin-bottom: 18px;
  }

  .prod-btn {
    width: 100%;
    background: transparent;
    border: 1px solid var(--border);
    color: var(--green);
    font-size: 11px;
    font-weight: 700;
    font-family: 'Inter', sans-serif;
    letter-spacing: 2px;
    text-transform: uppercase;
    padding: 10px 0;
    border-radius: 6px;
    cursor: pointer;
    transition: background .2s, border-color .2s;
  }

  .prod-btn:hover { background: var(--green-glow); border-color: var(--green-dim); }

  .product-card.pop { border-color: #1a3a1a; }
  .product-card.pop .prod-btn {
    background: var(--green);
    color: #000;
    border-color: var(--green);
  }
  .product-card.pop .prod-btn:hover { background: #2acc58; }

  /* ── FOOTER ── */
  footer {
    margin-top: 80px;
    border-top: 1px solid var(--border);
    padding: 28px 48px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    position: relative;
    z-index: 1;
  }

  .footer-logo {
    font-family: 'Rajdhani', sans-serif;
    font-size: 16px;
    font-weight: 700;
    letter-spacing: 2px;
    color: var(--muted);
    text-transform: uppercase;
  }

  .footer-logo span { color: var(--green); }

  footer p {
    font-size: 11px;
    color: var(--sub);
    letter-spacing: 2px;
    text-transform: uppercase;
  }

  .footer-links {
    display: flex;
    gap: 20px;
  }

  .footer-links a {
    font-size: 11px;
    color: var(--sub);
    text-decoration: none;
    letter-spacing: 1px;
    text-transform: uppercase;
    transition: color .2s;
  }

  .footer-links a:hover { color: var(--green); }

  @media (max-width: 768px) {
    nav { padding: 0 20px; }
    .hero h1 { font-size: 42px; }
    .featured-card { grid-template-columns: 1fr; }
    .features-box { grid-template-columns: 1fr; }
    footer { flex-direction: column; gap: 12px; text-align: center; }
    .nav-links { display: none; }
  }
</style>
</head>
<body>

<!-- NAV -->
<nav>
  <div class="nav-logo">Top<span>Cheats</span></div>
  <div class="nav-links">
    <a href="#">
      <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M3 9l9-7 9 7v11a2 2 0 01-2 2H5a2 2 0 01-2-2z"/></svg>
      Home
    </a>
    <a href="#products">
      <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="2" y="3" width="20" height="14" rx="2"/><line x1="8" y1="21" x2="16" y2="21"/><line x1="12" y1="17" x2="12" y2="21"/></svg>
      Products
    </a>
    <a href="#">
      <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/></svg>
      Feedback
    </a>
  </div>
  <div class="nav-right">
    <div class="nav-icon">
      <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 21v-2a4 4 0 00-4-4H8a4 4 0 00-4 4v2"/><circle cx="12" cy="7" r="4"/></svg>
    </div>
    <div class="nav-icon">
      <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="9" cy="21" r="1"/><circle cx="20" cy="21" r="1"/><path d="M1 1h4l2.68 13.39a2 2 0 002 1.61h9.72a2 2 0 002-1.61L23 6H6"/></svg>
    </div>
  </div>
</nav>

<!-- HERO -->
<section class="hero">
  <div class="hero-glow"></div>
  <h1>Top<span>Cheats</span></h1>
  <p>Buy access to the best configs &amp; scripts for ROBLOX</p>

  <div class="search-bar">
    <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg>
    <input type="text" placeholder="Search for products...">
  </div>

  <div class="hero-btns">
    <button class="btn-primary" onclick="document.getElementById('products').scrollIntoView({behavior:'smooth'})">Products</button>
    <button class="btn-green">Discord</button>
  </div>

  <div class="stats">
    <div class="stat-card">
      <div class="stat-num">200+</div>
      <div class="stat-lbl">Loyal Customers</div>
    </div>
    <div class="stat-card">
      <div class="stat-num">250+</div>
      <div class="stat-lbl">Products Sold</div>
    </div>
    <div class="stat-card">
      <div class="stat-num">24/7</div>
      <div class="stat-lbl">Support</div>
    </div>
    <div class="stat-card">
      <div class="stat-num">5.0★</div>
      <div class="stat-lbl">Rating</div>
    </div>
  </div>
</section>

<!-- FEATURED -->
<section class="featured-section">
  <div class="featured-card">
    <div>
      <div class="feat-badge">Featured</div>
      <div class="feat-title">UE Configs</div>
      <div class="feat-sub">High-Performance Rivals Config</div>
      <div class="feat-tags">
        <div class="feat-tag">
          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="20 6 9 17 4 12"/></svg>
          Top Accuracy
        </div>
        <div class="feat-tag">
          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="20 6 9 17 4 12"/></svg>
          Instant Delivery
        </div>
        <div class="feat-tag">
          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="20 6 9 17 4 12"/></svg>
          Regular Updates
        </div>
        <div class="feat-tag">
          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="20 6 9 17 4 12"/></svg>
          Undetected &amp; Safe
        </div>
      </div>
      <div class="feat-price-row">
        <div class="feat-price">$10</div>
        <button class="btn-green">Purchase Now</button>
      </div>
    </div>
    <div class="feat-img">
      <div class="feat-img-placeholder">
        <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><rect x="3" y="3" width="18" height="18" rx="2"/><circle cx="8.5" cy="8.5" r="1.5"/><polyline points="21 15 16 10 5 21"/></svg>
        Product Image
      </div>
    </div>
  </div>
</section>

<!-- FEATURES STRIP -->
<section class="features-strip">
  <div class="features-box">
    <div class="feat-left">
      <h3>Features</h3>
    </div>
    <div class="feat-right">
      <div class="feat-item">
        <div class="feat-dot">
          <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="20 6 9 17 4 12"/></svg>
        </div>
        Configs
      </div>
      <div class="feat-item">
        <div class="feat-dot">
          <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="20 6 9 17 4 12"/></svg>
        </div>
        Undetected
      </div>
      <div class="feat-item">
        <div class="feat-dot">
          <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="20 6 9 17 4 12"/></svg>
        </div>
        24/7 Support
      </div>
      <div class="feat-item">
        <div class="feat-dot">
          <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="20 6 9 17 4 12"/></svg>
        </div>
        Regular Updates
      </div>
    </div>
  </div>
</section>

<!-- PRODUCTS -->
<section class="products-section" id="products">
  <div class="section-header">
    <h2>All Products</h2>
    <p>Choose your config — instant delivery after purchase</p>
  </div>
  <div class="products-grid">

    <div class="product-card">
      <div class="prod-tag">Config</div>
      <div class="prod-name">UE</div>
      <div class="prod-price">$10 <span>USD</span></div>
      <div class="prod-per">per key</div>
      <button class="prod-btn">Purchase</button>
    </div>

    <div class="product-card pop">
      <div class="prod-tag" style="color:var(--green)">Most Popular</div>
      <div class="prod-name">Matcha</div>
      <div class="prod-price">$10 <span>USD</span></div>
      <div class="prod-per">per key</div>
      <button class="prod-btn">Purchase</button>
    </div>

    <div class="product-card">
      <div class="prod-tag">Config</div>
      <div class="prod-name">Matrix</div>
      <div class="prod-price">$10 <span>USD</span></div>
      <div class="prod-per">per key</div>
      <button class="prod-btn">Purchase</button>
    </div>

    <div class="product-card">
      <div class="prod-tag">Config</div>
      <div class="prod-name">Kicia</div>
      <div class="prod-price">$10 <span>USD</span></div>
      <div class="prod-per">per key</div>
      <button class="prod-btn">Purchase</button>
    </div>

    <div class="product-card">
      <div class="prod-tag">Fflags</div>
      <div class="prod-name">Fflags</div>
      <div class="prod-price">$5 <span>USD</span></div>
      <div class="prod-per">per key</div>
      <button class="prod-btn">Purchase</button>
    </div>

  </div>
</section>

<!-- FOOTER -->
<footer>
  <div class="footer-logo">Top<span>Cheats</span></div>
  <p>&copy; 2026 TopCheats — All rights reserved</p>
  <div class="footer-links">
    <a href="#">Discord</a>
    <a href="#">Support</a>
    <a href="#">Terms</a>
  </div>
</footer>


</body>
</html>